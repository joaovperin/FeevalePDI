import java.io.*;

File file = new File( basedir, "target/DigitalImageProcessing-1.0-SNAPSHOTxxx.jar" );
if ( !file.isFile() ){
    throw new FileNotFoundException( "Could not find generated JAR: " + file );
}
System.out.println("*** Verify OK.");